import {ChevronLeft,ChevronRight} from 'lucide-react';export default function Pagination({page,total,onChange}){if(total<=1)return null;const pages=[...Array(total)].map((_,i)=>i+1).filter(p=>p===1||p===total||Math.abs(p-page)<=1);return <nav className="pagination" aria-label="Pagination"><button disabled={page===1} onClick={()=>onChange(page-1)}><ChevronLeft/></button>{pages.map((p,i)=><span key={p}>{i>0&&p-pages[i-1]>1&&<i>…</i>}<button className={p===page?'active':''} onClick={()=>onChange(p)}>{p}</button></span>)}<button disabled={page===total} onClick={()=>onChange(page+1)}><ChevronRight/></button></nav>}
