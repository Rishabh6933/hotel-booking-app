import {useEffect,useState} from 'react';import {ArrowUp} from 'lucide-react';import {useLocation} from 'react-router-dom';export default function ScrollToTop(){const [show,setShow]=useState(false),{pathname}=useLocation();useEffect(()=>{window.scrollTo(0,0)},[pathname]);useEffect(()=>{const f=()=>setShow(scrollY>500);addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);return show&&<button className="scroll-top" onClick={()=>scrollTo({top:0,behavior:'smooth'})} aria-label="Scroll to top"><ArrowUp/></button>}
