import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {Search,MapPin} from 'lucide-react';export default function SearchBar({initial=''}){const [q,setQ]=useState(initial),navigate=useNavigate();const submit=e=>{e.preventDefault();navigate(`/hotels${q?`?search=${encodeURIComponent(q)}`:''}`)};return <form className="hero-search" onSubmit={submit}><MapPin/><label><span>Where do you want to stay?</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search by hotel name"/></label><button className="btn" type="submit"><Search size={19}/>Search stays</button></form>}
