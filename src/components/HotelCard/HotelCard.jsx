import {Link} from 'react-router-dom';import {Heart,MapPin,Star} from 'lucide-react';import {useFavorites} from '../../hooks/useFavorites';import {formatPrice,truncate} from '../../utils/format';
export default function HotelCard({hotel}){const {toggle,isFavorite}=useFavorites();return <article className="hotel-card"><div className="card-image"><Link to={`/hotels/${hotel.id}`}><img src={hotel.thumbnail} alt={hotel.name} loading="lazy"/></Link><button className={`heart ${isFavorite(hotel.id)?'saved':''}`} onClick={()=>toggle(hotel.id)} aria-label="Save hotel"><Heart fill="currentColor"/></button><span className="rating"><Star fill="currentColor"/> {hotel.rating}</span></div><div className="card-body"><p className="eyebrow"><MapPin/> {hotel.location}, India</p><Link to={`/hotels/${hotel.id}`}><h3>{hotel.name}</h3></Link><p className="description">{truncate(hotel.description)}</p><div className="card-foot"><div><strong>{formatPrice(hotel.price)}</strong><span> / night</span></div><Link to={`/hotels/${hotel.id}`}>View stay →</Link></div></div></article>}
