import {useState} from 'react';import {Link,NavLink} from 'react-router-dom';import {Heart,Menu,Moon,Sun,X} from 'lucide-react';import {useTheme} from '../../hooks/useTheme';import {useFavorites} from '../../hooks/useFavorites';
export default function Navbar(){const [open,setOpen]=useState(false);const {dark,toggle}=useTheme();const {ids}=useFavorites();return <header className="nav"><div className="container nav-inner"><Link to="/" className="brand"><span>H</span>Havenly</Link><nav className={open?'nav-links open':'nav-links'}><NavLink to="/" onClick={()=>setOpen(false)}>Home</NavLink><NavLink to="/hotels" onClick={()=>setOpen(false)}>Explore stays</NavLink><Link to="/hotels" className="favorites"><Heart size={18}/><span>Saved</span>{ids.length>0&&<b>{ids.length}</b>}</Link></nav><div className="nav-actions"><button className="icon-btn" onClick={toggle} aria-label="Toggle color theme">{dark?<Sun/>:<Moon/>}</button><Link className="btn btn-sm" to="/hotels">Find a stay</Link><button className="icon-btn menu" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div></div></header>}
