import {createContext,useContext,useState} from 'react';const C=createContext();export function FavoritesProvider({children}){const [ids,setIds]=useState(()=>JSON.parse(localStorage.getItem('favorites')||'[]'));const toggle=id=>setIds(v=>{const n=v.includes(id)?v.filter(x=>x!==id):[...v,id];localStorage.setItem('favorites',JSON.stringify(n));return n});return <C.Provider value={{ids,toggle,isFavorite:id=>ids.includes(id)}}>{children}</C.Provider>}export const useFavorites=()=>useContext(C);
